# Implementation Plans

Generated by the `improve` skill on 2026-07-13 against commit `cc8694e`.
The working tree already contains unrelated uncommitted changes; executors must
perform the drift check in each plan before editing. Execute in the order below.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make every project write safe and atomic | P1 | L | — | COMPLETED |
| 002 | Make history, project switching, and export revisions coherent | P1 | M | 001 | COMPLETED |
| 003 | Add a real desktop verification gate and reproducible CI | P1 | M-L | 001, 002 | COMPLETED |
| 004 | Establish one canonical layout/source contract | P1 | M | 001 | COMPLETED |

## Dependency notes

- Plan 001 must land first because Plans 002–004 exercise the same publish and
  rollback paths and must not preserve multiple incompatible implementations.
- Plan 002 depends on the common writer and safety checks from Plan 001.
- Plan 003 depends on the stable transport and revision contracts from Plans
  001 and 002 so its smoke tests assert the final behavior rather than mocks.
- Plan 004 can be implemented after Plan 001, but its fixtures should be used
  by Plan 003.

## Deferred work

Plans 001-004 are complete. The Rust backend smoke, macOS embedded-WebDriver
GUI path, final verification suite, and read-only review gate all pass.

- PDF pen/line-style fidelity, watcher performance, Git HEAD caching, and
  O(n²) geometry checker optimization are intentionally deferred until the
  safety and release gates are green.
- Windows/Jw_cad compatibility remains a release gate, not an automated local
  substitute.

## Findings considered and rejected

- Arbitrary user-selected import/export paths were not treated as a standalone
  vulnerability because the product intentionally exposes file dialogs. Plan
  001 still validates project-internal write destinations and symlinks.
